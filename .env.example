# Copy this file to .env and replace placeholders.
# Generate GAME_SPACE_ADMIN_PASSWORD_HASH from a plaintext password with:
# node -e "const { randomBytes, scryptSync } = require('node:crypto'); const password = 'replace-me'; const salt = randomBytes(16); const hash = scryptSync(password, salt, 64, { N: 16384, r: 8, p: 1 }); console.log('scrypt$' + salt.toString('base64') + '$' + hash.toString('base64'));"

GAME_SPACE_ADMIN_PASSWORD_HASH=scrypt$<saltBase64>$<hashBase64>
GAME_SPACE_ADMIN_SESSION_SECRET=<at-least-32-random-characters>
OPENAI_API_KEY=<openai-api-key-for-realtime-transcription>
CODEGEN_PROVIDER=codex
CODEGEN_CLAUDE_MODEL=claude-sonnet-4-6
CODEGEN_CLAUDE_THINKING=adaptive
